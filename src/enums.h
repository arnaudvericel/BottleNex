#ifndef ENUMS_H
#define ENUMS_H

enum class Motion
{
    Braking,
    Cruising,
    Accelerating
};

enum class VehicleType
{
    // todo : complete when other vehicle types are incorporated
    Car
};

#endif